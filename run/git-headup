#! /usr/bin/env node

var clc = require('cli-color')

var pipe  = '|'
var space = ' '

function write (string)
{
	process.stdout.write(string, 'utf-8')
}

var nl = '\n'

var output = clc.bold(pipe) + space + clc.bold(process.cwd()) + nl
write(output)
